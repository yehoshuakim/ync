[project]
name = "standin-mcp"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "mcp>=1.27,<2",
  "fastapi==0.138.0",
  "uvicorn[standard]>=0.32",
]

[project.optional-dependencies]
dev = ["pytest>=8.2"]


[tool.pytest.ini_options]
pythonpath = ["."]
